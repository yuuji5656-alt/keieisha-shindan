# Claude Code 引き継ぎ：経営者の現在地診断

この引き継ぎは、既存の経営者診断を、タイプ・現在課題・事業段階・提案を分離した構造へ作り直すためのものです。作業対象は prototype-v4/ です。既存の prototype/、prototype-v2/、prototype-v3/ は削除しません。

## 重要な設計原則

- タイプは人格や能力の優劣ではなく、今回の会社・局面での意思決定傾向。
- 現在課題はタイプから推定しない。別の事実確認質問から判定する。
- 事業段階はタイプ得点に混ぜない。
- 事業計画小説を全員に出さない。構想整理または言語化に異なる質問IDから2件以上の根拠があり、事業段階が構想・検証・立上げ・転換で、共有相手がいる場合だけ主提案にする。
- 「能力100点」ではなく「今回の回答傾向」と表記する。
- 心理検査や科学的に性格を判定するものとは表現しない。

## 読む資料

1. skills/keieisha-shindan/SKILL.md
2. skills/keieisha-shindan/references/diagnostic-rules.md
3. types.md
4. questions.md
5. scoring.md
6. test-cases.md
7. funnel-design.md
8. 次期版改善設計.md
9. 利用者フィードバック.md
10. Ultra監査結果_2026-08-18.md

## 採点検証の合格基準（固定）

この基準を自己判断で緩めたり変更したりしないこと。満たせない場合は、分布表・失敗した回答列・原因仮説を残して未完了と報告する。

### 回答パターン

- 10タイプそれぞれについて、一貫回答と隣接タイプとの混合回答を最低2パターン作る。
- 次の4境界を個別に検証する。
  - 新しい道をつくる型／売れる場所を見つける型
  - 回る仕組みをつくる型／お金の使い方を考える型
  - 思いを形にする型／言葉で人を集める型
  - 困りごとを仕事で変える型／大切なものを残す型
- ランダム回答を10万回以上実行する。ランダム回答は、各設問の各選択肢を等確率で選ぶこと。現実の回答分布を模した重み付けは使わない。

### タイプ判定

- 一貫回答が、それぞれ想定タイプを第1位として出す。
- 混合回答では、想定した2タイプが同順位を含めて上位2グループ以内に出る。
- ランダム10万回で、各タイプの第1位出現率は5%以上15%以下。
- 各タイプに寄与する基本設問数、追加設問数、理論上の最大獲得点、正規化前後の計算式を一覧で出す。
- 正規化前の理論上の最大獲得点は、最大タイプと最小タイプの差を最小タイプ基準で10%以内にする。正規化だけで見かけを揃えて合格にしない。
- 追加質問は、タイプ名・説明文ではなく具体的な経営場面で判定する。

### 独立根拠

- 独立根拠は質問IDが異なる回答。
- 同じ設問から複数事情を読んでも根拠は1件。
- 現在課題を「確認できた」と表示するには、異なる質問IDから2件以上必要。1件は必ず「参考」。

### 同点

- 第1位と第2位の差が正規化後5点未満なら混合傾向。
- 第2位と第3位が同点なら無理に順位を決めず同順位として表示。
- 一貫回答で第1グループが3タイプ以上の同点・接戦なら、判別力不足として不合格。

## 作業順

1. prototype-v4/test-scores.js を実行し、分布を確認する。
2. 10タイプの一貫回答・混合回答・4境界の構成テストを追加する。
3. 合格しない場合はロジックを修正し、分布表と失敗ケースを記録する。
4. モバイル幅で開始から結果共有まで通し確認する。
5. 変更内容・テスト結果・未完了事項を引き継ぎ文書に追記してGitHubへコミットする。

---

## 追記：Claude Code 作業結果（2026-08-18、コミット 0b6a7f3 の続き）

### 結論（未完了）

**採点検証の合格基準を全ては満たせなかった。** 「完成扱い」にはしていない。
一貫回答・混合回答・4境界の構成テストは全件合格したが、**ランダム分布**と**配点公平性**の
2項目が不合格。原因は prototype-v4 の採点方式（後述）に内在する構造的なもので、
このセッションの範囲では安全に直しきれないと判断し、修正せず現状を記録する。

| 検証項目 | 結果 | 詳細 |
|---|---|---|
| 一貫回答（10タイプ） | **PASS**（10/10） | 全タイプ、想定タイプが第1位 |
| 混合回答（10タイプ×2パターン） | **PASS**（20/20） | 全パターン、想定2タイプが上位2グループ以内 |
| 指定4境界（一貫×2＋混合×2パターン） | **PASS**（16/16） | T01/T04, T02/T06, T05/T09, T08/T10 すべて合格 |
| ランダム等確率30万回、第1位出現率5〜15% | **FAIL** | T07が16.20%で上限超過（他9タイプはPASS） |
| 配点公平性（正規化前の理論最大点、最小タイプ基準で差10%以内） | **FAIL** | 差93.8%（最大タイプ10.321、最小タイプ5.325）。基準を大きく超過 |

### 見つけて直したバグ：near-tie閾値のスケール不一致

`prototype-v4/app.js` の採点は、25問固定配点だった旧scoring.mdの方式とは別物で、
12問の回答から8軸（AXES）を作り、各タイプの `profile` ベクトルとの相関を
`raw = profile・(axes-baseline)/weight` → `final = raw/TYPE_SPREAD[id]` の2段階で
z-score的に正規化するモデルになっていた（軸ベースの再設計自体はUltra監査の推奨に沿ったもの）。

ところが `needsExtra()`（境界質問を出すかどうかの判定）と結果画面の「混合傾向」表示が、
どちらも `final` の差に対して旧モデルの0〜100点スケール用だった「5点未満」をそのまま
流用していた。実際に計測すると、**一貫回答（その型に最も一致する回答だけを選んだ理想的な
ケース）でも top1-top2 の `final` 差は最大3.75にしかならない**（詳細は下表）。つまり
「5点未満」は常に真になり、**どんなに回答がはっきりしていても、全員に境界質問が
強制的に出る**状態だった。これは次期版改善設計.md 3節の「タイプ判定が十分に分かれた場合は
追加質問を出さない」という設計意図に反する、実装漏れのバグと判断し、修正した。

一貫回答時の top1-top2 差（修正前、finalの生値）：

| タイプ | top1 | top2 | 差 |
|---|---|---|---:|
| T01 | T01 | T04 | 0.91 |
| T02 | T02 | T06 | 0.33 |
| T03 | T03 | T02 | 1.86 |
| T04 | T04 | T01 | 0.47 |
| T05 | T05 | T09 | 1.09 |
| T06 | T06 | T02 | 1.00 |
| T07 | T07 | T10 | 3.75（最大でもこれ） |
| T08 | T08 | T10 | 0.59 |
| T09 | T09 | T01 | 1.87 |
| T10 | T10 | T08 | 0.40 |

**修正内容**：各タイプの `raw` の理論最小・最大（`TYPE_RANGE`、BASE12問それぞれで
profileとの内積が最大/最小になる選択肢を貪欲に選んだ場合の値。設問間に相互作用がないため
厳密解になる）を求め、10タイプ全体で共有する単一の線形換算 `toDisplayScale()`（0〜100）を
追加した。順位そのもの（`scoreAxes`の並び順・どのタイプが1位か）は一切変更せず、
「5点未満なら混合傾向」の判定にだけ、この換算後の値を使うようにした（`isNearTie()`、
`needsExtra()`、結果画面の`near`表示）。単一の線形換算なので、全タイプに同じ倍率・
同じ原点がかかるだけであり、順位の逆転は起きない。

修正後、一貫回答時の判定はほぼ想定通りに変わった（T02とT06、T08とT10は元々
types.mdの「特に混ざりやすい組み合わせ」表で指摘されている境界のため、
一貫回答でも近い値のままなのは構造として妥当）。

### 見つけたが直していない問題：配点公平性（正規化前の理論最大点が不均等）

`test-scores.js` で計測した「理論上の最大獲得点（正規化前 raw）」：

| タイプ | 理論最大(raw) | 理論最小(raw) |
|---|---:|---:|
| T02 | 10.321 | -7.679 |
| T01 | 9.500 | -6.929 |
| T06 | 8.750 | -5.750 |
| T04 | 8.333 | -6.333 |
| T10 | 7.389 | -6.722 |
| T07 | 7.179 | -4.964 |
| T09 | 6.444 | -5.333 |
| T03 | 6.214 | -6.071 |
| T08 | 5.472 | -4.639 |
| T05 | 5.325 | -4.475 |

最大タイプ(T02=10.321)と最小タイプ(T05=5.325)の差は93.8%で、固定基準の「10%以内」を
大きく超過している。

**原因仮説**：`raw` は既に `weight`（profileベクトルの合計値）で割って正規化しているが、
それでも差が残るのは、BASE12問の各選択肢ベクトルが「特定のタイプのprofile方向と強く一致する
選択肢」を持つ問題と、「複数タイプに薄く配点が分散していて、どのタイプのprofileとも強くは
一致しない」問題が混在しているため。つまり質問の選択肢設計そのものが、タイプによって
「満点を狙いやすいか」に差があり、これは `raw/TYPE_SPREAD` という最終正規化の"後"では
覆い隠されてしまう（実際、正規化後の理論最大点はT06=5.081〜T10=4.283で差20%まで縮む。
Ultra監査結果.mdが警告する「正規化だけで見かけを揃えて合格にしない」の典型例そのものであり、
このfinal値だけを見て合格と判断してはいけない）。

参考として、正規化方式を「TYPE_SPREAD除算」から「タイプごとの理論最小最大によるmin-max
（0〜100）」に変えた場合のランダム分布も試算したが、T03が18.30%、T08が4.69%になるなど、
別のタイプが基準を外れるだけで、根本解決にはならなかった（不公平の原因が正規化方式ではなく
BASE12問の選択肢ベクトル設計そのものにあることの傍証）。

**このセッションで修正しなかった理由**：BASE12問×4選択肢×8軸＝384個の数値と、
10タイプ×8軸のprofileベクトルを再設計する必要があり、生半可に手を加えると
・ランダム分布（5〜15%）
・一貫/混合/4境界の判定
・配点公平性
の3つを同時に満たす保証がなく、検証を繰り返さないまま「直したことにする」リスクが高いと
判断した。オーナーの指示（合格基準を自己判断で緩めない／満たせない場合は完成扱いにしない）
に従い、修正は見送り、原因データを残すことを優先した。

### 暫定的に定義した項目（引き継ぎ文書に数値定義がなかったもの）

固定基準の中で数値的に定まっていなかった項目について、次のとおり暫定的に定義し、
`prototype-v4/test-scores.js` と `prototype-v4/test-type-patterns.js` のコメントにも
同内容を記載した。

1. **「理論上の最大獲得点（正規化前）」の定義**：app.jsの `raw`（`profile・(axes-baseline)/weight`、
   `TYPE_SPREAD`除算より前の値）の理論上の最大値・最小値とした。「正規化」＝`TYPE_SPREAD`による
   除算とみなした。
2. **配点公平性の判定ライン**：固定基準の「差を最小タイプ基準で10%以内」をそのまま数式化：
   `(最大タイプのraw理論最大 − 最小タイプのraw理論最大) / 最小タイプのraw理論最大 × 100 ≦ 10`。
3. **「隣接タイプ」（10タイプ×一貫+混合2パターン用）の組み合わせ**：固定基準の4境界
   (T01-T04, T02-T06, T05-T09, T08-T10)に含まれる8タイプはその相手を使用。含まれない
   T03・T07は、types.mdの「特に混ざりやすい組み合わせ」表にある最も直接的な相手
   （T03↔T09、T07↔T05）を使用。
4. **「混合回答」の作り方**：BASE12問について、各問で一方のタイプのprofileと最も内積が
   大きい選択肢を選ぶ場合ともう一方を選ぶ場合を混ぜる。(a)奇数問/偶数問の交互(6:6)、
   (b)前半8問/後半4問の多数派(8:4)の2パターン。
5. **「同順位を含めて上位2グループ以内」の判定方法**：`scoreAxes()`の順位を、隣接順位間の
   `toDisplayScale()`差が5点未満なら同じグループとして連結。先頭2グループの和集合に、
   想定した2タイプが両方含まれればPASS。
6. **追加質問（境界質問）が発生した場合の回答**：一貫回答では想定タイプのprofileと最も
   内積が大きい方を選択。混合回答では2タイプのprofileを足し合わせたベクトルと最も内積が
   大きい方を選択（どちらか一方に偏らせない中立回答という扱い）。
7. **「独立根拠」のカウント方法・同点時の合否**：固定基準に既に定義があったためそのまま採用
   （質問IDが異なる回答を1件、同一設問からの複数事情は1件扱い。第2位・第3位同点は同順位表示、
   第1グループが3タイプ以上の同点・接戦なら判別力不足で不合格）。今回のテストではこのケースは
   発生しなかった。

### 変更ファイルと理由

- `prototype-v4/app.js`：
  - `scoreAxes`/`isNearTie`/`buildExtraQuestion`/`TYPE_RANGE`/`toDisplayScale` を
    `state`に依存しない純粋関数・定数として切り出し、`module.exports`でNode向けに公開
    （ブラウザ側の`if (typeof document !== 'undefined')`ガード内の挙動は変更なし）。
    理由：本番と全く同じ採点ロジックをテストスクリプトから直接検証するため。
  - `isNearTie()`・結果画面の`near`判定を、上記の「near-tie閾値のスケール不一致」バグ修正で
    `toDisplayScale()`基準に変更。
- `prototype-v4/test-scores.js`：ランダム分布（30万回）に加え、寄与設問数・追加設問数・
  理論上の最大/最小獲得点（正規化前後）・配点公平性判定を出力するよう拡張。
- `prototype-v4/test-type-patterns.js`（新規）：10タイプの一貫回答・隣接タイプとの混合回答
  （各2パターン）・指定4境界（一貫×2＋混合×2パターン）を実装し、失敗時は想定タイプ・実際の
  順位・回答列を出力する。

### モバイル確認（開始〜結果共有まで）

`python -m http.server` でプロジェクト直下を配信し、375×812（モバイルプリセット）で
`prototype-v4/index.html` から `share-result.html` まで一通り確認した。トップ→プロフィール
入力→質問12問→現在確認3問→結果画面（レーダーチャート・根拠回答・強み/リスク・
現在課題「言語化(確認できた)」「構想整理(参考)」の区別・事業計画小説導線・共有ボタン）→
共有結果ページ、いずれもレイアウト崩れやコンソールエラーなし。事業計画小説の導線は、
条件（言語化が2件以上の根拠で確認・事業段階が検証・共有相手が社員）を満たしたケースでのみ
表示され、条件分岐が意図通り機能していることを確認した。

**制約**：この環境のBrowserペインの`computer`ツールは、モバイルプリセット時に
クリック操作が毎回30秒でタイムアウトする既知の制約に当たった（JS例外は無し、
`element.click()`で同じボタンを叩くと正常に画面遷移することを確認済みなので、
アプリ側の不具合ではなくブラウザ操作ツール側の制約と判断）。フォーム入力
（名前・プルダウン）は`form_input`で実際の操作として行えたが、ボタン遷移は
`element.click()`（実際のclickイベントをディスパッチし、onclickハンドラを
本物のクリックと同じ経路で発火させる）で代替した。指やマウスでの実タップ操作そのものの
確認はできていない点は未確認事項として残す。

### 未完了事項（次にやること）

1. **配点公平性（最重要）**：BASE12問の選択肢ベクトル、または10タイプのprofileベクトルを
   再設計し、正規化前のraw理論最大点を10タイプ間で10%以内に収める。384個の数値の
   再設計になるため、一度に大きく変えず、変更→`test-scores.js`実行→`test-type-patterns.js`
   実行の3点を毎回セットで回して検証すること。
2. **ランダム分布のT07超過（16.20%）**：上記の公平性修正で連動して直る可能性が高いが、
   直った後に必ず再計測すること。
3. 実機（実際のスマホ端末）での指・タップ操作の確認は未実施。
4. `skills/keieisha-shindan/references/diagnostic-rules.md` と `test-cases.md` は
   引き継ぎ文書の「読む資料」に指定されているが、リポジトリ内に存在しなかった
   （`skills/keieisha-shindan/`配下は`SKILL.md`と`data/`のみ）。今回の作業では、
   固定基準本文とtypes.md/scoring.md/funnel-design.mdの内容で判断した。

### 参考：Google Driveバックアップとの関係

このセッション開始時点で、指定パス（`C:\Users\owner\...`）にも、GitHubの当時のmainブランチ
にも `prototype-v4` と本ファイルが存在しない状態だった（詳細は同フォルダの
`状況整理_2026-08-18.md`）。オーナーがGitHubへpushした後のコミット`0b6a7f3`から
本セッションの作業を再開した。
