# 経営者診断サイトの進捗共有ルール

## 基本方針

- GitHubの `main` ブランチを制作データの唯一の最新版とする。
- Googleドライブはバックアップとし、GitHubと内容が異なる場合はGitHubを優先する。
- 同じ時間に2台のパソコンから同じファイルを編集しない。
- Codexの会話だけで決まった重要事項は、制作ファイルまたは文書へ反映してから作業を終了する。

## 作業開始時

毎回、変更を始める前に次を実行する。

```powershell
git pull
git status
```

開始時にCodexは、次の内容を短く報告する。

- 取得した最新コミット番号
- 未保存の変更の有無
- 今回変更する予定の範囲

未保存の変更や競合がある場合は、新しい編集を始める前に内容を確認する。

## 作業中

- 重要な判断、保留事項、次回作業は文書へ残す。
- 既存機能へ影響する変更は、変更前後の動作を確認する。
- 公開サイトの変更では、スマホ表示を優先して確認する。
- 作業範囲が変わる場合は、勝手に広げず利用者へ報告する。

## 作業終了時

変更内容と確認結果を見直してから、GitHubへ保存する。

```powershell
git status
git add 対象ファイル
git commit -m "変更内容を日本語で記入"
git push
```

Codexは作業終了時に必ず次を報告する。

1. 今回変更した内容
2. 実際に確認・テストした内容と結果
3. 未完成、未確認、次に行うこと
4. 最新コミット番号
5. GitHubへのpush結果
6. 公開ページの確認結果（公開サイトを変更した場合）
7. Googleドライブの更新状況

実行していない確認を「確認済み」と報告しない。GitHubへのpushやGoogleドライブ更新ができなかった場合は、そのまま明記する。

## 別のパソコンへ移るとき

1. 作業中のパソコンで変更をコミットしてpushする。
2. 終了報告に最新コミット番号を残す。
3. 移動先のパソコンで `git pull` を実行する。
4. 移動先のコミット番号が終了報告と一致することを確認する。
5. `git status` で未保存の変更がないことを確認してから再開する。

## 例外

- 調査や相談だけでファイル変更がない場合は、コミットとpushは不要。
- 作業途中を保存する必要がある場合は、未完成であることが分かるコミットメッセージと終了報告を残す。
- Googleドライブは自動同期とは限らないため、更新していない場合は必ず「未更新」と報告する。
